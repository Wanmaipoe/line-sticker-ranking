// Twitter/X card image. Reuses the exact same generator as the OG image so the two
// can never drift. summary_large_image cards expect the same 1200x630 dimensions.
export { default, alt, size, contentType } from './opengraph-image';
